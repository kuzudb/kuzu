load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "processor_tests",
    srcs = [
        "operator/property_reader/node_property_reader_test.cpp",
        "operator/scan/scan_test.cpp",
    ],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/processor",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
