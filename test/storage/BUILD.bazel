load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "property_column_test",
    srcs = ["property_column_test.cpp"],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/storage:structures",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)

cc_test(
    name = "adj_rels_test",
    srcs = ["adj_rels_test.cpp"],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/storage:structures",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
