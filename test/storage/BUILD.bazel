load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "storage_tests",
    srcs = [
        "adj_rels_test.cpp",
    ],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/storage:structures",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
