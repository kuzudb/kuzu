load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "column_test",
    srcs = ["column_test.cpp"],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/storage:column",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)

cc_test(
    name = "adjedges_index_test",
    srcs = ["adjedges_index_test.cpp"],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/storage:indexes",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
