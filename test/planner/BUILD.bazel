load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "binder_tests",
    srcs = [
        "query_graph_test.cpp"
    ],
    linkstatic = 1,
    deps = [
        "//src/planner:binder",
        "//src/loader:loader",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
