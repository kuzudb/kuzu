load("@rules_cc//cc:defs.bzl", "cc_test")

cc_test(
    name = "common_tests",
    srcs = [
        "vector/node_vector_test.cpp",
    ],
    copts = [
        "-Iexternal/gtest/include",
    ],
    deps = [
        "//src/common:common",
        "@gtest",
        "@gtest//:gtest_main",
    ],
)
